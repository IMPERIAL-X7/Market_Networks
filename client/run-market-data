#!/bin/sh
# Launches a Market-Data Client: ./client/run-market-data 127.0.0.1 5000 JNST
set -e
here=$(dirname "$0")
root=$(cd "$here/.." && pwd)

if [ ! -x "$root/bin/market_data_client" ]; then
    make -C "$root" >&2
fi

exec "$root/bin/market_data_client" "$@"
